export * from '@/lib/media/types';

// Backward compatibility mappings if needed
export type MediaFormatOption = import('@/lib/media/types').MediaFormat;
export type MediaMetadata = import('@/lib/media/types').MediaResult;
export type JobStatus = import('@/lib/media/types').JobState;
